# Người dùng (global — mọi dự án)

- Làm tại CÔNG TY CỔ PHẦN DỊCH VỤ T-VAN HILO (Hilo), trụ sở 18 Đoàn Trần Nghiệp, Hai Bà Trưng, Hà Nội. Conventions chi tiết theo từng repo nằm trong `AGENTS.md` của repo đó.
- Giao tiếp: trả lời **tiếng Việt**, **ngắn gọn**; code, identifier, commit message bằng tiếng Anh.
- Kỹ thuật: React 19 best practices (SRP, composition) ưu tiên hơn né breaking change; dùng **Tailwind CSS** thay inline style (màu dynamic = CSS vars + arbitrary values); search-before-code; đọc `docs/` của repo trước khi sửa code.
