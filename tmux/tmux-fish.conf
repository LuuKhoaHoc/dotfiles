# Source main tmux config (oh-my-tmux) for theme and keybindings
source-file ~/.tmux.conf

# Fish-specific overrides
set -g default-terminal "tmux-256color"
set -g default-shell /usr/bin/fish
set -g default-command ''
