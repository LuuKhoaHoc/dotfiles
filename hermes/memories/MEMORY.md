Arch Linux + Hyprland on NVMe. User khoahoc. Docker-heavy dev setup (erp microservices). Terse caveman response style preferred. HR workspace aliases: `@hr/shared/` → `apps/hr/src/shared/`, `@hilo/shared` (package) → `packages/shared`, `@/` → `apps/hr/src/`. Add both tsconfig.app.json paths + vite.config.ts resolve.alias.
§
Arch Linux + Hyprland on NVMe. User khoahoc. Docker-heavy dev setup (erp microservices). Terse caveman response style preferred.
§
Tiny-nvim tự động load .nvim-config.lua ở thư mục hiện tại nếu tồn tại. File này dùng để setting project-specific như biến môi trường, LSP, path alias, keymap, v.v.