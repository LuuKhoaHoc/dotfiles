-- Example .nvim-config.lua for Odoo project
-- Copy this file to your Odoo project root as .nvim-config.lua
-- It will be auto-loaded by init.lua without being tracked by git

-- Optional: Set LSP on-demand
-- vim.g.lsp_on_demands = { "eslint" }

-- Optional: Configure project-specific settings
-- vim.opt.expandtab = true
-- vim.opt.shiftwidth = 4

-- Optional: Custom keymaps for this project
-- local keymap = vim.keymap.set
-- keymap("n", "<leader>ot", function()
--   vim.cmd("!python odoo-bin -d dbname -u all")
-- end, { desc = "Odoo: Update all modules" })
